# Keep real API keys server-side only.
# For the prototype, leave PROVIDER as mock.
VITE_LLM_PROVIDER=mock

# Future server-side only keys. Do not expose in VITE_ variables.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
